# NiceTransfer — Development Overview

Source and issues: [github.com/joko-zauberzeug/nicetransfer](https://github.com/joko-zauberzeug/nicetransfer)

---

## Philosophy

NiceTransfer is deliberately minimal. The goal is a tool you can clone, run, and understand — no build pipeline, no package structure, no framework beyond NiceGUI. Everything needed to run lives in the project directory.

---

## Project files

| File | Purpose |
|------|---------|
| `nicetransfer.py` | The entire application — UI, server, MCP, REST endpoints |
| `nicetransfer.css` | Component styles loaded once at startup, served as inline `<style>` |
| `MANUAL.md` | User-facing documentation, rendered at `/manual` |
| `CHANGELOG.md` | Version history, rendered at `/changelog` |
| `DEVELOPMENT.md` | This file, rendered at `/development` |
| `config.toml` | Runtime configuration (generated by `install.sh`) |
| `install.sh` | Sets up the venv, generates `config.toml` and `run.sh` |
| `run.sh` | Activates the venv and starts `nicetransfer.py` (generated by `install.sh`) |

The Markdown and CSS files are read at request time, not baked in — edit them while the server is running and the next page load reflects the change.

---

## Design concepts

### Token model

Every request — browser, download, preview, MCP, AI discovery — requires `?token=TOKEN`. The token is printed in the startup banner and regenerates on each start unless a fixed value is set in `config.toml`. There are no public endpoints except NiceGUI internals. This is why every generated link in the code includes the token.

### Server vs. client

NiceTransfer distinguishes between the *server* (the device running the app, accessing via `127.0.0.1`) and *clients* (other devices on the same network). The server gets the full control panel; clients see only the file sections. Client permissions (delete, trash access) are configurable.

### Sections

Four file sections — Share, Upload only, Download only, Trash — each backed by a configurable directory. Deleted files always go to Trash first; nothing is removed immediately. Sections can be toggled at runtime via the control panel.

---

## AI integration

### Discovery

NiceTransfer embeds AI discovery hints in every page `<head>`:

```html
<meta name="mcp-server"      content="http://<ip>:<port>/mcp?token=...">
<meta name="mcp-server-card" content="http://<ip>:<port>/.well-known/mcp/server-card.json?token=...">
<meta name="llms-txt"        content="http://<ip>:<port>/llms.txt?token=...">
```

These endpoints are dynamic — served by the running instance, token-protected, generated from current state. An AI that opens any NiceTransfer URL finds them automatically.

A second path targets terminal-based agents (like Claude Code): the startup banner printed to stdout contains the token and a direct link to `/llms-local.txt`. An AI running `./run.sh` reads that output and can immediately fetch its operating instructions — no QR code, no browser needed.

`/llms.txt` documents the HTTP API and available operations for AI assistants. `/llms-local.txt` (server-only) adds extended development instructions. Both are generated at request time from `nicetransfer.py`.

In practice, AI access currently works from the server device — a desktop AI like Claude Code can connect directly. Remote clients on the same Wi-Fi can reach the browser UI, but most hosted AI assistants can't reach a local network address. As local AI apps and on-device models become more common, the discovery layer is already in place.

### HTTP-first interface philosophy

The primary AI interface is **plain HTTP + `llms.txt`** — not MCP.

`llms.txt` documents every HTTP endpoint: what it does, how to call it, what it returns. An AI reads this file and makes plain HTTP requests. No MCP client, no protocol handshake, no prior configuration required.

MCP Streamable HTTP is technically also plain HTTP under the hood (JSON-RPC over POST). But calling it directly without a configured MCP client means constructing JSON-RPC envelopes manually — more complexity, no benefit over a simple REST call. A configured MCP client hides that complexity, but requires setup that HTTP doesn't.

**Operator controls are not part of the AI interface.** Section toggles, permissions, and timeout belong in the NiceGUI Control panel — reactive, real-time, operator-facing. The AI works within whatever the operator has configured, not as an operator itself. This is why there is no `PATCH /state` endpoint.

### MCP as optional layer

The MCP server (`/mcp`) is available for AI clients that natively support it — a bonus, not the primary path. Some operations overlap between HTTP and MCP (list, upload, download, shutdown); both call the same underlying Python functions, so there is no duplicated logic.

When AI clients implement dynamic MCP discovery via the `<meta name="mcp-server">` tag already present on every page, the HTTP/MCP distinction will matter less. That infrastructure is already in place.

---

## AI development guides

For working on NiceTransfer with an AI assistant:

- **`llms-nicetransfer.md`** — NiceTransfer-specific conventions, architecture constraints, and commit workflow, written for language models
- **`llms.md`** — NiceGUI framework reference for AI assistants
- **`CLAUDE.md`** — loads both files for Claude Code sessions

These files are the source of truth for AI-assisted development. This document is the human-readable counterpart to `llms-nicetransfer.md`.
